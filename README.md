# MoDex — GitHub Pages

## Run locally
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
```

## GitHub Pages
Push this repository to GitHub on the `main` branch. The included GitHub Actions workflow builds and deploys the site automatically.

In GitHub:
Settings → Pages → Source → GitHub Actions
